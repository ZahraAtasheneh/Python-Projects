import random


class Square:
    info = 'something'

    def __init__(self, name):
        self.name=name
        self.lucky_number=random.randint(1,10)

my_shape= Square('book')
#my_shape=

print(f'{self.lucky_number}, {self.name}')
